# Rust
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# Risc0
.risc0/
*.bin

# CMake / Qt build
build/
build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
# Ignore CMake-generated Makefiles under build dirs but keep the
# project-root Makefile (dev wrappers, tracked).
**/build/Makefile
**/build-*/Makefile
# Stage-copy of metadata.json used by moc for Q_PLUGIN_METADATA
app/whistleblower/src/metadata.json
moc_*.cpp
qrc_*.cpp
ui_*.h
*.qmlc
*.jsc
*.lgx

# Nix
result
result-*

# Editors & OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Env
.env
.env.local
*.local

# Node (for any GUI tooling)
node_modules/
dist/

# Logs
*.log

# Demo artifacts (regenerable)
artifacts/
.demo-state
.basecamp-data/

# nwaku + storage local data
nwaku-store/
storage-data/

# External repos cloned for recon (not part of submission).
# Match both the directory form and a symlink at the root.
_external
_external/
*.lgx
